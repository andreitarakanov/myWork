package inheritance;

public class MarbleCollection {
/***	private ArrayList<Marble> = new ArrayList<Marble>();
	//Part a
	public int getTotalMarbles() {
		int temp = 0;
		for(int i = 0;i<sets.size();i++)
		{
			temp += sets.get(i).getNumber();
		}
		return temp;
	}
	//Part b
	public int removeColor(String marbleCol) {
		String temp = "";
		int num = 0;
		for(int i = sets.size();i>0;i--)
		{
			if(sets.get(i).getColor().contains(marbleCol)==true) {
				num+=sets.get(i).getNumber();
				sets.remove(i);
			}
				
		}
		return num;
	}
	***/
}
